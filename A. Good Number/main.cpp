#include <iostream>

using namespace std;

int main()
{
   int n,m;
   cin>>n>>m;
    char arr[m+1];
    char tmp[m+1];
   for(int i=0; i<n; i++)
   {
       if(i==0)
       {
           cin>>arr[i];
       tmp[i]=ar[i];
       }
       else
       {
           cin>>arr[i];
           for(int j=0; j<=m; j++)
           {
               if(arr[i]==tmp[j])
               {
                   counter
               }
           }
       }
   }
    return 0;
}
